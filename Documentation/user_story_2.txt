Title: Display GIF search results

As a user,
I want to see the search results as a grid of images,
So that I can browse through the relevant GIFs.

Acceptance Criteria:
- The application displays the search results as a grid
- Each grid cell contains a thumbnail image of a GIF
- The application uses a custom CSS grid with flexbox for organizing the images
